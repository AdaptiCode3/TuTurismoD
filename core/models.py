"""
core/models.py
==============
⚠️  RESTRICCIÓN ARQUITECTÓNICA — ESTE ARCHIVO DEBE PERMANECER VACÍO ⚠️

Este proyecto utiliza PyMongo directamente para interactuar con MongoDB.
NO se deben definir modelos heredando de django.db.models.Model.

La definición de esquemas/validaciones de documentos se realiza mediante
clases de repositorio ubicadas en cada app (ej: core/repositories.py).
"""
# No importar django.db.models
